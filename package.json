{
  "name": "component-error-boundary",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "组件级错误边界：自定义边界 + IndexedDB + Web Worker（降级 UI / 重试 / 日志 / 异常提示）",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
